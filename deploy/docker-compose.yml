services:
  app:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "127.0.0.1:8002:8001"
      - "127.0.0.1:8507:8506"
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
